---
spec: signal-log-and-ci-autodetect
basePath: specs/signal-log-and-ci-autodetect
phase: design
task: 0/0
updated: 2026-05-14
---

# Progress: signal-log-and-ci-autodetect

## Original Goal

Signal Event Log + CI Auto-Detection — Address gaps C2 (HOLD signals ignored) and C4 (CI snapshot separation) from ENGINE_ROADMAP.md

## Completed Tasks

- [x] Research completed (554 lines) — found at specs/signal-log-and-ci-autodetect/research.md
- [x] Requirements completed (rewritten 2026-05-14, aligned with ENGINE_ROADMAP §C2/C4 and harness-engineering docs; OpenHands immutable event log pattern + Deep Agents marker auto-detection; added US-4 Auditability/Replay; promoted schema upgrade from out-of-scope to FR-8) — implicitly approved on advancing to design phase.
- [x] Design completed (2026-05-14) — found at specs/signal-log-and-ci-autodetect/design.md (3 mermaid diagrams, 10 key decisions, 6 create / 9 modify file inventory, test coverage table). Awaiting approval.
- [x] 1.4 Schema: add signals.lastProcessedLine field (schema:signals_added)
- [x] 1.5 Schema: upgrade ciCommands from string[] to {command,category} (schema:cicmds_upgraded)
- [x] 1.6 Schema: add ciSnapshot per-category result map (schema:ci_snapshot_added)
- [x] 1.7 [VERIFY] Schema sanity — all checks passed (schema:verified)
- [x] 1.8 Create templates/signals.jsonl seed file (schema:template_created)

## Current Task

Awaiting design approval. Resolved blocker: fd 202 collision -> user ratified Option (a) refactor stop-watcher baseline lock to fd 204 as Implementation Step 0 (prerequisite). Requirements AC-1.4 preserved unchanged.

## Learnings

Research identifies two core problems:
1. **Gap C2**: HOLD/PENDING/URGENT signals checked via fragile grep on chat.md text — needs structured signals.jsonl
2. **Gap C4**: CI commands discovered manually from Verification Contract — needs auto-detection script

### Design-phase additions (2026-05-14)
- **fd 202 conflict** ✅ RESOLVED: stop-watcher baseline lock will be refactored from fd 202 -> fd 204 as Implementation Step 0 (prerequisite). signals.jsonl.lock keeps fd 202 as requirements AC-1.4 mandates. Decision D3 in design.md updated accordingly.
- **discover-ci.sh already classifies by category** (test/lint/build/typecheck) — composition with detect-ci-commands.sh just needs a `(command, category)` tuple-aware dedupe, not a re-classification step.
- **chat.md grace cycle**: legacy `[HOLD]` markers must keep working for one release. Detection: grep finds, AND jq active-count is 0 → honour the grep result and log WARN once per spec.
- **Hash-stability test**: append-only invariant is only enforceable at review time via `sha256sum -c` snapshot. Runtime detection is not possible (would require kernel-level inode auditing). Document as operator-level invariant.
- **Bats 1.13.0 confirmed** on PATH. Existing tests in `tests/*.bats` pass cleanly — safe to add `tests/signal-log.bats` and `tests/ci-autodetect.bats` without touching existing test infra.
- **`signals.lastProcessedLine` is for audit-only**, not for the active-signal query. The query always rescans the full file (correct per AC-1.2). Cursor is only useful for `diff-since-last-iter` log entries.

### Task-planning additions (2026-05-14)
- **Task count finalised**: 63 top-level tasks at `fine` granularity. Per-phase: P1=26 (POC, ~41%), P2=6 (refactor, ~10%), P3=24 (testing, ~38%), P4=4 (quality gates, ~6%), P5=3 (VE E2E, ~5%). Slight overweight on Phase 3 vs the suggested 25-30% because the Test Coverage Table in design.md has 17 distinct rows requiring 1 bats test each plus 5 cadence VERIFY checkpoints — the architect's coverage discipline drives the count, not planner padding.
- **Single-atomic-task for HOLD-check replacement (1.19)**: the planner-spec requires landing both `commands/implement.md` and `hooks/scripts/stop-watcher.sh` jq replacements in the same commit so `main` never observes a mixed state where one entry point reads chat.md and the other reads signals.jsonl. Done-when explicitly diff-asserts the two engine files contain the byte-identical jq query string.
- **Phase 2 lib-signals.sh extraction**: planner deliberately leaves Phase 1 with inline shell snippets (in implement.md and stop-watcher.sh), then refactors them into `hooks/scripts/lib-signals.sh` in Phase 2. This keeps the POC committable and verifiable at every step without premature abstraction (Karpathy rule 2). The VE2 (Phase 5) task assumes `lib-signals.sh` exists — depends on Phase 2 landing.
- **No VE0 (ui-map-init)**: project type is CLI tool / coordination engine, `UI Present: No`. VE0 ui-map-init is browser-only and explicitly omitted. VE1-VE3 use filesystem + CLI verification only — no browser automation, no Playwright skills required.
- **Plugin version bump 5.0.0 -> 5.1.0** (minor — new feature) lives in a single Phase 4 task (4.1) touching both `plugins/ralphharness/.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json`, never scattered.
- **Bats tests come last in Phase 3** but each Phase 1/2 implementation task has runnable Verify steps using `bash -n` (shell syntax) and `jq -e .` (JSON validity) so we never lose a verification gate. Phase 3 tests then assert behavioural invariants.
- **Risk area to watch during execution**: Task 1.19 (atomic HOLD-check swap) is the highest-risk change because it modifies both engine entry points. Recommend the executor run it with `bash -n` AND a manual smoke (paste a HOLD signal into a real spec's signals.jsonl, run a sub-shell version of the gate) before committing. The Done-when's byte-identical-query diff is the structural guard.
- **`tests/replay-signals.bats` is a new bats file** (not just an addition to existing files). Task 3.21 creates both the script `hooks/scripts/replay-signals.sh` and its test in the same task because the test is the only way to verify the deterministic file-order tie-break behaviour.

## Blockers

- None. All open questions either resolved (Q1 fd allocation) or non-blocking (Q2 severity field YAGNI, Q3 pytest canonicalisation, Q4 lastProcessedLine purpose, Q5 replay tie-break).

## Next

Awaiting tasks.md approval. Run /ralphharness:implement to begin Phase 1 (POC) execution.
### Adversarial-review revision pass (2026-05-15)
- **stop-watcher had NO existing chat.md HOLD gate** — 1.19 ADDS the gate at the anchor line `MAX_TASK_ITER=$(jq -r '.maxTaskIterations // 5' ...)`. Only `commands/implement.md` had the legacy grep gate (line ~304). Earlier draft was wrong on this; fixed.
- **One-shot migrator pattern** (new task 1.18a + revised 1.18): `migrate-state.sh` is the SINGLE call-site for the legacy `ciCommands` string-to-object rewrite. Other readers either source it or document why they don't need to. Audited: stop-watcher only does `jq empty` corruption check (doesn't touch ciCommands shape — safe).
- **Block-marker contract**: tasks 1.17/1.19/1.20/1.25/2.6 wrap their inserted code with `# BEGIN <NAME>` / `# END <NAME>` markers. Tasks 1.27 / VE2 / 3.22 extract by these markers for E2E execution. This is the workaround for "slash commands cannot be invoked from non-interactive bash".
- **Era-aware test 3.23**: byte-identical inline-string assertion holds in Phase 1; switches to "both files call active_signal_count" after 2.1 extracts the lib. Test detects era at runtime via `grep -l active_signal_count`.
- **3.21 (replay-signals.sh) MUST call migrate-state.sh first** — added per the loader-site rule from 1.18a.
- **Doc-test consistency 1.23 ↔ 3.20**: verification-layers.md keeps ONE sentence about the legacy `[HOLD]` grace fallback so test 3.20 stays doc-consistent. The "primary path is signals.jsonl" wording isn't removed; the grace fallback sentence is preserved.
- **Atomic-landing staging guard added to 1.19 Verify**: `git diff --cached --name-only | grep -c '(implement\.md|stop-watcher\.sh)$' == 2` runs BEFORE `git commit`. Prevents accidentally committing only one of the two files.
- **Task count revised**: 64 → 65 (added 1.18a). Phase 1: 27 → 28. `.ralph-state.json.totalTasks` updated to 65.
