---
spec: harness-enforcement-gates
basePath: specs/harness-enforcement-gates
phase: research
task: 2/14
updated: 2026-05-19
---

# Progress: harness-enforcement-gates

## Original Goal

Investigar y diseñar mecanismos de **enforcement automático** para el plugin
RalphHarness que aborden 5 deficiencias detectadas en el diagnóstico forense de
la spec `mutation-score-ramp` (proyecto ha-ev-trip-planner). El diagnóstico
completo está en `plans/mutation-score-ramp-diagnostic.md`.

Puntos a investigar:

1. **2.3 — Task 1.18 saltado, DEADLOCK no resuelto**: El coordinador saltó una
   tarea `[VERIFY]` obligatoria (1.18) y avanzó a tareas posteriores sin resolver
   el DEADLOCK que el external-reviewer escaló dos veces. Consecuencia material:
   `__init__` quedó en 50.7% vs 51% threshold. Falta un **bloqueo secuencial**
   que impida delegar la tarea N+1 mientras una tarea `[VERIFY]` precedente
   siga en `[ ]`.

2. **3.1 — Fix incompleto de timeout (index_manager.py)**: El coordinador afirmó
   haber corregido un loop infinito, pero `git diff HEAD` mostró CERO cambios en
   el archivo de producción. Pista del usuario: el problema puede ser que
   `git diff` ya no muestre el cambio porque está en un commit o en stage —
   `git diff` a secas puede NO ser suficiente como mecanismo de verificación.
   Investigar qué comparación git es robusta para confirmar que un fix afirmado
   existe realmente en el código (working tree + staged + committed en la rama).

3. **3.3 — Phase A gate NO verde, se procedió a Phase B igualmente**: El
   `design.md` definía que Phase A debe alcanzar gate verde antes de Phase B,
   pero con 2 módulos por debajo del umbral la ejecución avanzó de fase de todos
   modos. Falta un **gate de fase** enforced mecánicamente por el coordinador.

4. **4.2 — `.metrics.jsonl` vacío**: El archivo de métricas existía pero quedó
   con una sola línea vacía; no se registró ninguna métrica pese a que la spec
   trataba precisamente sobre métricas de mutation testing.

5. **4.3 — `_unmark_task.py` vacío**: El script placeholder existía pero nunca
   se implementó. Relacionado con la deficiencia 2.2 (desmarcado manual de
   tareas verificadas). Debería validar que una tarea no tiene entrada PASS en
   `task_review.md` antes de permitir desmarcarla.

Entregable de esta fase: un **plan de investigación** que analice cada punto,
su causa raíz en el plugin, y las opciones de enforcement viables.

## Completed Tasks

- [x] 1.1 Create verify-fix-present.sh helper script skeleton - 922e3c8
- [x] 1.2 Implement base-ref resolution (merge-base + checkpoint SHA fallback) - 922e3c8
- [x] 1.3 Three-state diff + optional pattern check - d76901b

## Current Task

Awaiting next task

## Completed Tasks

- [x] 1.4 [VERIFY] Quality checkpoint: shellcheck N/A (not installed), bash -n PASS, smoke: exit 0 (changed+pattern present), exit 1 (unchanged), exit 2 (pattern absent), exit 3 (base unresolvable, verified in 1.2) -- no fixes needed
- [x] 1.5 Append gate_verify_sequential() function to stop-watcher.sh
- [x] 1.6 Add DEADLOCK signal emission to gate_verify_sequential() — appends active DEADLOCK control signal to signals.jsonl on VERIFY block; read-only fs degrades to WARN+return 0
- [x] 1.7 Wire gate_verify_sequential() call into loop-control block — inserted before HOLD-GATE; non-zero → exit 0 (no continuation)
- [x] 1.8 Add append-only invariant assertion to stop-watcher.sh loop — appended before VERIFY gate; exits 1 on deletion; 14 insertions, 0 deletions
- [x] 1.13 Add `emit_task_metric` call line inside loop-control block — appended after continuation prompt build; syntax check PASS, call on line 872

## Current Task

Tasks phase complete and approved (design implicitly approved).
52 tasks across 5 phases. Next: execution.

## Learnings

- Master root cause for ALL 5 points: "prose-as-enforcement" — rules live as
  English in agent/reference markdown where the LLM can skip them, not as
  deterministic shell gates in hooks.
- The coordinator's ONLY advancement gate is `taskIndex < totalTasks`
  (`stop-watcher.sh:676`). No sequential `[VERIFY]` gate, no phase-exit gate.
- `implement.md:290-292` explicitly TOLERATES task skipping ("No correction:
  allow execution to continue") when `taskIndex > completed`.
- `signals.jsonl` is mechanically enforced (HOLD-GATE); `chat.md` is NOT. The
  §2.3 DEADLOCK landed in `chat.md` so no mechanical gate fired. Fix: route
  reviewer DEADLOCK to `signals.jsonl`.
- `_unmark_task.py` is NOT plugin code — zero hits in `plugins/ralphharness/`.
  It was a spec-local artifact. "Implementing it" = designing an integrity
  guard the plugin currently lacks.
- `write-metric.sh` is production-ready and flock-safe; metrics are empty only
  because the ONLY caller is an LLM-discretionary coordinator prompt block
  (`implement.md:680-708`). Fix: move the call into `stop-watcher.sh`.
- `git diff HEAD` (working-tree) and `git diff HEAD~1` (single commit) each
  cover only some states; robust fix-presence = `git merge-base` three-state
  diff (committed ∪ staged ∪ working-tree).
- Reusable machinery already exists: PRE-EXEC-GATE pattern, `flock` fds
  (200/201/202), HOLD-GATE, circuit breaker. The fix is to WIRE it, not invent.
- `stop-watcher.sh` is append-only by policy (loop-safety.md Decision 3).
- Coordinator auto-recovery (diagnostic §3.2) is NOT one of the 5 points and
  conflicts with circuit-breaker "manual reset only" — recommend out of scope.

## Interview Responses

### Requirements Interview (from requirements.md)
- Point 1 (VERIFY gate): Block ALL preceding unsatisfied `[VERIFY]` tasks — no
  deferral marker. The plugin has no notion of deferral; keep it simple/strong.
- Point 3 (Phase gate): Synthetic `[VERIFY] Phase X exit gate` task emitted by
  task-planner as the last task of each phase. Reuses Point 1's gate; no
  coupling to metric semantics.
- Point 5 (Un-mark guard): Tiered escalation. On illegitimate `[x]`→`[ ]`
  detection: (1) emit DEADLOCK to HALT advancement; (2) BEFORE escalating to a
  human, run a full subagent review/triage to filter false positives — if the
  subagent understands the problem and can resolve it, resolve it; (3) only
  escalate to the human when it is a genuine conflict needing human
  intervention. No blind auto-revert.
- Point 4 §3.2 coordinator auto-recovery: OUT of scope. Conflicts with
  circuit-breaker "manual reset only"; deserves its own spec.
- Chosen approach: Full feature set — all 5 enforcement gates, each as a
  deterministic shell gate (NFR enforcement-as-shell).

## Learnings (requirements phase)

- 6 user stories: 5 per diagnostic point + 1 cross-cutting "enforcement-as-shell"
  story (US-6) to make NFR-1 first-class and testable.
- 19 FRs, 7 NFRs. Point 5 expands to FR-13..FR-18 because tiered escalation
  (halt / subagent triage / conditional human) is 3 distinct mechanical steps.
- Open design questions deferred: (a) mark-snapshot persistence location
  (.task-marks.prev vs state field — defer to engine-state-hardening conventions);
  (b) which subagent runs Tier 2 integrity triage and its autonomous-resolution
  authority bounds; (c) exit-gate naming when a phase already ends with a
  [VERIFY] checkpoint — recommend always appending a distinct gate task.
- Scope decision encoded: §3.2 coordinator auto-recovery explicitly Out-of-Scope.
- verify-fix-present.sh needs a base-ref fallback when origin/main is unreachable
  (recorded checkpoint SHA) — flagged as AC-2.7 + an Escalate-if condition.

### Design Interview (from design.md)
- Mark snapshot persistence: `.ralph-state.json` field (e.g. `taskMarkSnapshot`),
  not a sidecar file — single source of truth, follows engine-state-hardening
  conventions, atomic with other state.
- Tier 2 integrity triage agent: use the `bmad-consensus-party` skill if
  available (exact skill name confirmed present at
  `.claude/skills/bmad-consensus-party/`). It runs BMAD Party Mode iteratively
  until agents reach consensus. Fallback when the skill is absent: invoke 2–3
  subagents (e.g. external-reviewer + qa-engineer) that perform a consensus-
  seeking review of the un-mark.
- Exit-gate naming: task-planner ALWAYS appends a distinct
  `[VERIFY] Phase X exit gate` task, even when the phase already ends with a
  `[VERIFY]` checkpoint — unambiguous mechanical marker.
- Chosen approach: Append-only shell gates extending existing infrastructure
  (`stop-watcher.sh` functions + one helper script), no new control flow.

## Learnings (design phase)

- 5 gates designed: 3 appended `stop-watcher.sh` functions
  (`gate_verify_sequential`, `emit_task_metric`, `gate_task_mark_integrity`),
  1 standalone helper (`verify-fix-present.sh`), 1 task-planner emission rule.
- Append-only nuance: in-loop gates need a one-line *call* inside the existing
  loop-control `if`-body. This mirrors the existing in-block `source
  lib-signals.sh` line and the HOLD-GATE call convention — NOT a control-flow
  edit. Flagged in design Unresolved Questions; executor should escalate only
  if a strict reading of loop-safety.md Decision 3 forbids even call insertion.
- `taskMarkSnapshot` `.ralph-state.json` field shape: `{schemaVersion,
  checkedTaskIds[], externalUnmarks{}, capturedAt}`. Legacy `// null` → fresh
  snapshot + return 0 (first run cannot detect a regression).
- Metrics duplicate prevention needs `lastMetricTaskIndex` / `lastMetricIteration`
  state fields — idempotency guard for exactly-one-line-per-advancement.
- `verify-fix-present.sh` exit-code contract: 0 present / 1 unchanged /
  2 pattern absent / 3 base unresolvable (exit 3 = the documented ESCALATE).
- FR-12: the implement.md metrics block is REMOVED outright (not demoted) —
  a kept fallback risks the duplicate line FR-12 forbids.
- task-planner exit-gate emission is artifact-generation-as-prose, which is
  permitted; NFR-1 forbids enforcement-as-prose, and the enforcement is the
  shell gate US-1 that *reads* the emitted task.
- File count: 9 create (1 helper script + 5 bats + 3 implied) / 7 modify.
- bats verified: Bats 1.13.0 on PATH, smoke run clean, 14 existing suites in
  plugins/ralphharness/tests/. No package.json test script — invoke bats direct.
- Version bump: 5.6.0 → 5.7.0 (minor — feature).

### Tasks Interview (from tasks.md)
- Task granularity: fine (40-60+ atomic tasks, [VERIFY] checkpoints every 2-3).
- E2E verification: YES — add a final end-to-end gate-integration task that
  drives a fixture spec through `stop-watcher.sh` with all gates wired, asserting
  the gates fire/halt as designed (beyond the 5 per-gate bats unit suites).
- Chosen approach: Thorough — full POC-first 5-phase plan (POC wiring → refactor
  → 5 bats suites → quality gates/PR), quality checkpoints throughout.

## Learnings (tasks phase)

- 39 tasks across 5 phases (Phase 1: 25, Phase 2: 5, Phase 3: 13, Phase 4: 5,
  Phase 5: 4). POC milestone is task 1.24 (all 5 gates wired end-to-end).
- Each phase ends with a distinct `[VERIFY] Phase X exit gate` task (1.G, 2.5,
  3.13, 4.G, 5.G) — interview decision 3, applied to this tasks.md itself.
- Dependency order encoded: verify-fix-present.sh first (helper, no deps), then
  the 3 stop-watcher functions, then agent-file re-points that consume them, then
  bats suites (Phase 3) that test the wired state.
- Append-only invariant is asserted 4× (tasks 1.8, 1.19, 2.3, 5.3) via
  `git diff | grep -c '^-'` == 0 — turns the loop-safety.md Decision 3 prose rule
  into a mechanical check at every checkpoint.
- E2E task (3.12) is a `cli`-style bats test driving a real fixture spec dir
  through stop-watcher.sh — NOT a browser task (project type `cli`).
- Risk area: the 3 in-block call-line insertions (1.7, 1.13, 1.18) are the only
  edits to existing flow. Executor must escalate if a strict reading forbids even
  the call insertion (HOLD-GATE precedent says it is permitted).
- task 1.10 is self-referential — it edits task-planner.md (this agent's own
  definition) to add the exit-gate emission rule.

### Tasks Review (spec-reviewer)
- REVIEW_PASS — 12/12 dimensions, 0 critical issues. 52 tasks, 5 phases.
- 4 non-blocking INFO notes: Phase 5 is a benign superset of the 4-phase model;
  AC-5.5 Tier-3 has no bats case (correct per design Mock Boundary); task 3.12
  adds a 6th bats file (E2E) not in the design File Structure table (additive,
  sound); task 1.24 cites US-1..US-6 rather than concrete FRs (cosmetic).

## Blockers

- None currently

## Next

Complete research, then proceed to requirements

## Implement Phase (started)
- State merged: phase=execution, totalTasks=52, executionPhase=poc
- Git checkpoint: 9c42d7b7cd
- CI commands discovered: 3 items (all test category)
- External reviewer: not yet configured (task_review.md absent)
- Pending: onboarding questions, execution loop

- [x] 1.9 Add taskMarkSnapshot schema to .ralph-state.json merge — 9f6e806
- [x] 1.10 Add phase exit-gate emission rule to task-planner.md — f1795d4
- [x] 1.11 [VERIFY] Quality checkpoint: agent-file edits well-formed — FIX: added DEADLOCK→signals.jsonl append_signal instruction to external-reviewer.md (was missing)
- [x] 1.12 Append emit_task_metric() function with advancement detection, write_metric call, and lastMetricTaskIndex idempotency guard

<!-- REVIEWER INTERVENTION [task-1.9] 2026-05-19T12:05:49Z -->
REVIEWER: task-1.9 status=FAIL
criterion_failed: FR-4, AC-1.6 — external-reviewer.md missing append_signal for DEADLOCK→signals.jsonl
fix_hint: Add instruction in DEADLOCK escalation section to also append DEADLOCK control signal to signals.jsonl via append_signal. Required: import/call append_signal from lib-signals.sh for DEADLOCK.
<!-- END REVIEWER INTERVENTION -->
## Completed Tasks
- [x] 1.14 Remove LLM-discretionary metrics block from implement.md — 413f90d
- [x] 1.15 [VERIFY] Quality checkpoint: metrics wiring syntax + smoke — no fixes needed
- [x] 1.17 Wire integrity gate into loop + Tier 2 triage instruction — gate_task_mark_integrity call inserted after HOLD-GATE; <task_mark_integrity> section added to spec-executor.md
- [x] 1.19 [VERIFY] Quality checkpoint: integrity gate syntax + append-only — bash -n PASS, zero deletions in stop-watcher.sh and spec-executor.md, no fixes needed
- [x] 1.20 Add Tier 2 integrity-triage DEADLOCK handler to implement.md — 2cebb7a
- [x] 1.21 Wire Tier 2 resume (FALSE_POSITIVE: jq signal resolution + .progress.md log) + Tier 3 human escalation (GENUINE_CONFLICT: escalation block + awaitingApproval=true) — to_be_produced
- [x] 1.22 Re-point spec-executor.md post-commit check to verify-fix-present.sh — to_be_produced
- [x] 1.23 Re-point implement.md Layer 3 review to verify-fix-present.sh
- [x] 1.24 POC milestone — all 5 gates wired end-to-end
- [x] 1.G [VERIFY] Phase 1 exit gate — all 24 tasks [x]; bash -n PASS on both scripts; executionPhase updated to "refactor"
